import base.List;
public class Sort {
	//those stubs currently have to be created, since we don't know in all data types whether sorting is activated in config
	public static <T extends Comparable<T>> void bubbleSort(List<T> input){
	}
	
	public static <T extends Comparable<T>> void quickSort(List<T> input){
	}
	
	private static <T extends Comparable<T>> int partition(List<T> input, int left, int right){
		return -1;
	}
}
